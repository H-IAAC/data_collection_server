<div class="center">
    <h1>Experimentos</h1>
</div>
<div>
    <% for (var i=0; i < experimentos.length; i++) { %>
            <div class="center">
                <div class="card">
                    <div class="card-container">
                        <h3>
                            <b>
                                <%= experimentos[i].label %>
                            </b>
                            <i onclick="onClickDelete('<%= experimentos[i].label %>')" class="material-icons"
                                style="float: right;">delete_forever</i>
                        </h3>
                        <h4>
                            <%= experimentos[i].activity %>
                        </h4>
                        <p>
                            <%= new Date(experimentos[i].time).toString() %>
                        </p>
                        <p>
                            <a onclick="onClickCard('<%= experimentos[i].label %>')" href="#">Conteúdo: <%=
                                experimentos[i].content %>
                                    arquivos</a>
                        </p>
                        <% if (experimentos[i].error > 0) { %>
                            <p style="background-color:red; text-align: center;">Post Processor Error! Check the 'err.log' file.</p>
                        <% } %>
                        <% if (experimentos[i].videoAvailable === false) { %>
                            <p><button onclick="location.href='/upload?experiment=<%= experimentos[i].label %>'">Upload</button></p>
                        <% } else { %>
                            <p><button onclick="onClickWatch('<%= experimentos[i].label %>')">Watch</button></p>
                        <% } %>
                    </div>
                </div>
            </div>
            <% } %>
</div>
<section class="bottom">
    <div class="center">
        <button onclick="window.history.back()" class="btn">Back</button>
    </div>
</section>

<script type="text/javascript">

    function onClickCard(label) {
        window.location.href = '/experimentos/download?label=' + encodeURI(label);
    }

    function onClickWatch(label) {
        window.location.href = '/experimentos/watch?label=' + encodeURI(label);
    }

    function onClickDelete(label) {
        let text = "Remover arquivos relacionados ao " + label + "?";
        if (confirm(text) == true) {
            alert('TODO');
        }
    }

</script>